# Sentence-level provenance for `experiments_draft.tex`

The experiment text is newly written from the repository's recorded results. The sources below provide rhetorical structure only; they do not provide evidence for PSPFlow's numerical claims. Numerical values, protocol statements, and limitations are taken from the local result JSON files, the repository evaluator, and the project data summary.

The baseline citation keys used by the LaTeX section are supplied in [`experiments_bib_additions.bib`](experiments_bib_additions.bib); merge these entries into the manuscript bibliography if the keys are not already present.

Primary structural sources:

- [Conv-TT-LSTM, NeurIPS 2020](https://proceedings.neurips.cc/paper_files/paper/2020/file/9e1a36515d6704d7eb7a30d783400e5d-Paper.pdf), especially Sec. 4 and Figs. 2--3.
- [CWAEE, NeurIPS 2023](https://proceedings.neurips.cc/paper_files/paper/2023/file/5b84864ff8474fd742c66f219b2eaac1-Paper-Conference.pdf), especially Secs. 4.4--4.5.
- [ICLR 2025 structural-entropy paper](https://proceedings.iclr.cc/paper_files/paper/2025/file/92ce40962b4098f7bf6eed33128fc606-Paper-Conference.pdf), especially Sec. 5.4.
- [BERT-DS, Nature Biomedical Engineering](https://www.nature.com/articles/s41551-023-01093-3), the ablation-study setup paragraph.
- [FaceLit, CVPR 2023](https://openaccess.thecvf.com/content/CVPR2023/papers/Ranjan_FaceLit_Neural_3D_Relightable_Faces_CVPR_2023_paper.pdf), the quantitative-comparison and qualitative-results paragraphs.

## Prose sentences

Each item gives the exact sentence in the draft, followed by the structural source and the source sentence or location that supplied its organization. The source wording is quoted only in short fragments.

| ID | Draft sentence | Structural source |
|---|---|---|
| E01 | "We evaluate PPG-to-ECG synthesis on the processed MIMIC-IV Waveform dataset using 10-s windows of 1,250 samples acquired at 125~Hz." | Conv-TT-LSTM, Sec. 4, evaluation setup: "We test our proposed models on ... datasets." Dataset/task first, then the concrete evaluation unit. |
| E02 | "The preprocessing uses a fixed within-subject split, with windows randomly split within each subject." | BERT-DS, Nature Biomedical Engineering, ablation protocol: "we decided to set a static train--test split and random seed before ... all models." The fixed split is a local protocol fact. |
| E03 | "At inference time, every target window is paired with a distinct reference window from the same subject, while the target ECG is withheld from the inference pathway." | Flow Matching, Sec. 3.2, conditional setup: conditioning observations are introduced before the generative objective; the target-free inference detail is local implementation evidence. |
| E04 | "Unless stated otherwise, we report per-window z-score MAE and RMSE, Fr\'echet distance (FD), and HR-MAE, where lower values indicate better performance." | Conv-TT-LSTM, Sec. 4, "Evaluation": define the evaluation quantities before reporting results. |
| E05 | "The evaluator computes FD after a joint, unwhitened PCA projection to 64 dimensions, adds $10^{-4}$ to each covariance diagonal, and averages three trials." | Conv-TT-LSTM, Sec. 4, implementation-details pattern: specify the evaluation implementation immediately after naming the metrics. All parameter values are from the repository metrics implementation. |
| E06 | "The HR-MAE score follows the repository's legacy protocol: the generated ECG is cleaned at a hard-coded 128~Hz, whereas the real ECG is not filtered before peak extraction." | Conv-TT-LSTM, Sec. 4, implementation-details pattern: state the concrete evaluation procedure after naming the metric. The asymmetry is a local evaluator caveat. |
| E07 | "We therefore use this metric for direct comparison with the released baselines and interpret it as a protocol-specific diagnostic rather than a calibrated clinical heart-rate estimate." | Nature Biomedical Engineering, BERT-DS ablation paragraph: explicitly delimit what a controlled comparison does and does not establish. |
| E08 | "We compare against all independently implemented baselines in the repository: CardioGAN, CLEP-GAN, RDDM, CardioFlow, and UniCardio." | Conv-TT-LSTM, Sec. 4.1, "we compare ... with ... baseline": name the comparison set before interpretation. |
| E09 | "CardioGAN uses an attention-based adversarial generator with time- and frequency-domain discriminators, CLEP-GAN combines contrastive and adversarial learning with attention gating, RDDM uses a region-guided diffusion process, CardioFlow uses peak-aware rectified flow, and UniCardio uses a unified diffusion transformer." | Conv-TT-LSTM, Methodology overview: introduce a method by listing its modules and their roles; the sentence compresses each baseline's repository README description. |
| E10 | "All implementations use the same normalized 125~Hz, 1,250-sample MIMIC-IV protocol and the shared evaluator." | Conv-TT-LSTM, Sec. 4, implementation-details paragraph: state the common experimental configuration before comparisons. |
| E12 | "Table~\ref{tab:main-results} summarizes the comparison." | CWAEE, Sec. 4.4: "The results are summarized in Table 1." |
| E13 | "PSPFlow obtains the lowest value for every reported metric, with MAE of 0.37, RMSE of 0.64, FD of 2.66, and HR-MAE of 1.90." | CWAEE, Sec. 4.4: "From the results, it can be found that our method consistently outperforms the compared methods ..." The values are local results. |
| E14 | "Relative to the strongest external method, CardioFlow, PSPFlow reduces MAE by 35.3\%, RMSE by 29.5\%, FD by 28.7\%, and HR-MAE by 27.4\%." | NeurIPS 2020, Sec. 4.1: comparison discussion follows the table with explicit relative performance interpretation. The percentages are calculated from Table~\ref{tab:main-results}. |
| E15 | "These gains are observed simultaneously in pointwise waveform agreement, distributional similarity, and beat-rate consistency, which is consistent with the intended separation of PPG-observable and ECG-private information." | CWAEE, Sec. 4.4, performance interpretation after the table; the physiological interpretation is specific to this manuscript. |
| E16 | "Figure~\ref{fig:benchmark-metrics} presents the same comparison using four fixed linear-axis panels." | Conv-TT-LSTM, Fig. 2 caption: "Figure 2: SSIM v.s. LPIPS scores ..."; quantitative results are restated as a visual comparison. |
| E17 | "The gap is particularly pronounced for FD: PSPFlow reduces FD by 28.7\% relative to CardioFlow and 98.1\% relative to CLEP-GAN, while retaining the lowest pointwise and HR-MAE errors." | CWAEE, Sec. 4.4, table-driven interpretation: identify the metric with the clearest separation and then state the remaining metrics. Percentages are local calculations. |
| E19 | "We conduct an ablation study to isolate the contribution of patient context and shared--private factorization." | ICLR 2025, Sec. 5.4: "We conduct ablation studies ... to evaluate the impact ... of each module." |
| E20 | "We compare the basic latent rectified-flow baseline, which uses neither patient context nor the shared--private strategy, with a patient-context-only variant and PSPFlow, which combines patient context with shared--private ECG factorization." | ICLR 2025, Sec. 5.4, module replacement description: "We ablate the two key modules ... by replacing them with alternatives." |
| E21 | "All three variants use the same four reported metrics and the same lower-is-better convention." | Conv-TT-LSTM, Sec. 4, common evaluation setup before ablations. |
| E22 | "As shown in Table~\ref{tab:ablation}, adding patient context to the basic model reduces MAE, RMSE, FD, and HR-MAE by 15.3\%, 11.2\%, 10.4\%, and 2.3\%, respectively." | CWAEE, Sec. 4.5.1: "The results with standard deviations are presented in Table 3"; table-first ablation interpretation. Percentages are local calculations. |
| E23 | "Adding the shared--private factorization on top of the patient-context-only variant produces a further reduction of 29.1\%, 26.4\%, 64.7\%, and 24.7\% on the same metrics." | ICLR 2025, Sec. 5.4, incremental-module interpretation: "Combining ... further improves the performance." Percentages are local calculations. |
| E24 | "The largest incremental change occurs in FD, supporting the interpretation that explicitly reserving an ECG-private subspace improves the distribution of generated morphologies rather than merely reducing pointwise error." | NeurIPS 2020, Sec. 4.1, metric-specific discussion after a comparison table; the ECG-private interpretation follows the proposed model's design. |
| E25 | "Relative to the basic baseline, PSPFlow reduces MAE by 40.0\%, RMSE by 34.6\%, FD by 68.3\%, and HR-MAE by 26.4\%." | NeurIPS 2020, Sec. 4.1, explicit relative-improvement reporting after the main comparison. Percentages are local calculations. |
| E26 | "Figure~\ref{fig:qualitative-waveforms} shows representative 10-s windows together with the input PPG, the target ECG, and outputs from the principal waveform baselines." | FaceLit, CVPR 2023, Fig. 3 caption: "Figure 3. Qualitative results" followed by a comparison with another method; the panel ordering is adapted to this task. |
| E27 | "PSPFlow follows the target beat timing while retaining sharper local complexes and smoother inter-beat morphology than the comparison outputs in these examples." | Conv-TT-LSTM, Fig. 3 discussion: "Our method generates ... visually crisp images, compared to other approaches." This is an observation from the generated figure. |
| E28 | "The visual comparison is consistent with the quantitative results: CardioFlow preserves the broad rhythm but exhibits larger local deviations, whereas RDDM and CLEP-GAN show more visible amplitude and morphology discrepancies." | Conv-TT-LSTM, Fig. 3 discussion: "Overall, our model produces sharper frames and better preserves ... details"; the waveform-specific observations are local. |
| E29 | "These examples are illustrative rather than a substitute for the full-test metrics in Table~\ref{tab:main-results}." | FaceLit, CVPR 2023, qualitative-results convention: qualitative examples accompany, rather than replace, quantitative evaluation. |
| E30 | "The reported split is within-subject, and the reference window is sampled from the same split without enforcing temporal precedence; consequently, these experiments measure personalized interpolation within observed subjects rather than unseen-patient or causal historical generalization." | BERT-DS, Nature Biomedical Engineering, fixed-split disclosure; the generalization boundary is a local property of this dataset protocol. |
| E31 | "In addition, the frozen reference encoders used by PSPFlow were pretrained on the training pool, including the fine-tuning validation subset, so the validation split is not an end-to-end pristine holdout." | Nature Biomedical Engineering, ablation fairness paragraph: state what was fixed or previously exposed before interpreting validation. |
| E32 | "Finally, the available results establish waveform and heart-rate fidelity on MIMIC-IV, but they do not by themselves establish downstream diagnostic performance or three-dataset generalization; those claims require additional labeled datasets and task-specific evaluations." | CWAEE, Sec. 4.4, which reports standard classification results and then introduces separate evaluations for different target properties; the scope restriction is required by the local evidence. |
| E33 | "For PSPFlow, we report the deterministic mean predictor from \\texttt{stage\\_rep/best.pt}, selected using training-only validation MAE plus RMSE; the learned flow residual is omitted because it performed worse on that validation split." | Conv-TT-LSTM, Sec. 4, implementation-details and model-selection pattern: identify the evaluated variant and selection protocol before interpreting results. The checkpoint and validation boundary are local checkpoint evidence. |

## Caption provenance

- Main-results caption: Conv-TT-LSTM and CWAEE table-caption pattern ("Table ... Comparison ..."; "The results are summarized ...").
- Benchmark figure caption: Conv-TT-LSTM Fig. 2 caption, which defines axes/metric direction after identifying the comparison plot; the horizontal four-panel layout, per-panel value ordering, and decimal tick formatting are local visualization choices.
- Ablation-table caption: ICLR 2025 Sec. 5.4 and CWAEE Table 3, which name the ablated components and the full configuration.
- Ablation-figure caption: RDDM Fig. 6, which identifies the plotted ablation quantity and the selected configuration; the 100\% normalization is computed from the local ablation results.
- Qualitative-waveform caption: FaceLit, CVPR 2023, Fig. 3 qualitative-comparison caption, which places generated outputs beside a reference; the standardization note is local plotting metadata.
